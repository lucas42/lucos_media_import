services:
  media_importer:
    build: .
    container_name: lucos_media_import
    volumes:
      - "media:/medlib/ceol srl"
      - "state:/var/state"
    environment:
      - SYSTEM
      - MEDIA_API
      - MEDIA_DIRECTORY=/medlib/ceol srl
      - MEDIA_PREFIX
      - KEY_LUCOS_MEDIA_METADATA_API
      - LOGANNE_ENDPOINT
      - TRACK_KEY
      - SCHEDULE_TRACKER_ENDPOINT
      - STATE_DIR
    image: lucas42/lucos_media_import:${VERSION:-latest}
    restart: always
    healthcheck:
      test: ["CMD", "pgrep", "cron"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
  test:
    build: .
    container_name: lucos_media_import_test
    image: lucas42/lucos_media_import:${VERSION:-latest}
    command: pipenv run python test.py
    profiles: [test]

volumes:
  media:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=aurora.lan,ro,nolock,hard,intr,retry=3"
      device: ":/medlib/ceol srl"
  state: